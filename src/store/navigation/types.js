export const NAME_SPACE = 'navigation';

const types = {
  NAVIGATE: `${NAME_SPACE}/NAVIGATE`,
  RESET: `${NAME_SPACE}/RESET`,
  GO_BACK: `${NAME_SPACE}/GO_BACK`,
  POP: `${NAME_SPACE}/PUSH`,
};

export default types;
